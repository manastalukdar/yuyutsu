import 'package:drift/drift.dart';

// This is a stub that should never be called directly
QueryExecutor openConnection() {
  throw UnsupportedError('This should not be called directly. Use conditional imports.');
}